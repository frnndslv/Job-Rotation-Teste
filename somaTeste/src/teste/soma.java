package teste;

public class soma {
    public static void somaMetodo() {
        int indice = 13, Soma = 0, K = 0;

        while (K<indice ) {
            K = K + 1;

            Soma = Soma + K;

        }

        System.out.println( Soma );
    }
}

